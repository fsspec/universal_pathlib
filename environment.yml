name: upath
channels:
    - defaults
    - conda-forge
dependencies:
    - python==3.8
    - fsspec==0.8.4
    # optional
    - requests
    - s3fs
    - jupyter   
    - ipython
    - pytest
    - pylint
    - flake8
    - pyarrow
    - moto
    - pip
    - pip:
      - hadoop-test-cluster
      - nox
    